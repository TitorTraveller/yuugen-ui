import { style } from "@vanilla-extract/css";

export const card = style({
	backgroundColor: "rgba(9, 255, 0, 0.47)",
	boxShadow: "0px 4px 6px rgba(0, 0, 0, 0.1)",
	padding: "20px",
	borderRadius: "10px",
});